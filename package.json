{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "Smart family expense tracking and management system",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install --legacy-peer-deps && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "docker:up": "docker-compose up",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "concurrently": "^8.2.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/yogitakeswani26/expense-tracker.git"
  },
  "author": "yogitakeswani26",
  "email": "yogitakeswani26@gmail.com",
  "license": "MIT",
  "keywords": [
    "expense-tracker",
    "family-finances",
    "budget-management",
    "react",
    "node",
    "mongodb"
  ]
}
